# JavaScript

## Usage
```
# Install required npm packages
$ npm i

# Run the unit tests once
$ npm run test

# Run the unit tests and watch for code changes, and re-run every time there is a change
$ npm run test:watch 
```